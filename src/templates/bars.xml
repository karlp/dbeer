<?xml version="1.0" encoding="UTF-8"?>
<bars>
    {% for bar in bars -%}
    <bar lat="{{bar.bar.lat}}" lon="{{bar.bar.lon}}" osmid="{{bar.bar.bar_osm_id}}">
        <name>{{bar.bar.name}}</name>
        <type>{{bar.bar.type}}</type>
        <distance>{{bar.distance}}</distance>
        <prices>
            {%- for price in bar.prices -%}
            <price drinkid="{{price}}">{{bar.prices[price]}}</price>
            {%- endfor -%}
        </prices>
        <lon_bucket>{{bar.bar.lon_bucket}}</lon_bucket>
    </bar>
    {% endfor -%}
</bars>